# Environment secrets — never commit real keys
.env
.env.local
.env.*.local

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Anchor / Solana build artifacts (large binaries, do not commit)
target/

# Local devnet test wallets and token lists — contain private keys, not for production
scripts/test-payer.json
scripts/test-tokens.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
